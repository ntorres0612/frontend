var ip = '127.0.0.1'
module.exports = {

  // wsType: 'ws',
  // ws: `${ip}:4020`,
  // host: `http://${ip}:4020`,
  maxigiros_messenger: `http://${ip}:5001`,
  // zodiaco_signaling: `http://${ip}:4024`,
  // socketRoot: `http://${ip}:4021`,
  // pathSocketSignaling: ``,
  pathSocketMessenger: ``,
  host: 'http://127.0.0.1:8080/',
  baseServer: 'http://127.0.0.1:7612/',
  urlPrinter: 'http://localhost:4000/',
  printServiceLocalIp: 'http://localhost:3500/',
}